export type Picture = {
  pictureId: number;
  image: string;
  userName: string;
  createdDate: Date;
};

export type PictureDetail = {
  image: string;
  userId: string;
  userName: string;
  createdDate: Date;
};
